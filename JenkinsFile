pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                cmakeBuild buildDir: 'build', buildType: 'Debug', generator: 'MinGW Makefiles', installation: 'InSearchPath', label: '3.22.3_autoinstall', sourceDir: 'D:\\Jenkins\\workspace\\pipeline-project-C\\Pattern\\', steps: [[args: 'clean all'], [args: 'install', envVars: 'DESTDIR=${WORKSPACE}\pipeline-project-C\Pattern']]
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
    }
}
