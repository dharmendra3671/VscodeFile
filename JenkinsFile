pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'cmakeBuild buildDir: 'build', buildType: 'Debug', cleanBuild: true, installation: 'InSearchPath', label: 'build_C', sourceDir: r'D:\\Test\\Testfile\\src', steps: [[args: 'install', envVars: 'DESTDIR=${WORKSPACE}/path/to/dir']]'
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
       
    }
}
